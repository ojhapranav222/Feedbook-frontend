'use client';

import React from 'react';

const RightSidebar = () => {
  return (
    <aside className="hidden lg:block fixed top-16 right-0 w-72 h-full p-4">
      <h2 className="font-semibold text-text-secondary">Contacts</h2>
      {/* Placeholder for contacts list */}
    </aside>
  );
};

export default RightSidebar;